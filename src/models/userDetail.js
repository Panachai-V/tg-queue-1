export default class UserDetail {
    constructor(prefix, firstname, lastname, phone) {
      this.prefix = prefix,
      this.firstname = firstname,
      this.lastname = lastname,
      this.phone = phone
    }
  }